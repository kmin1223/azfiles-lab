# Never commit the source support-wiki PDFs (Microsoft Confidential / NDA)
*.pdf.source
source-wikis/
*Overview.pdf

# Old/working copies
docs/_archive-old/
*_flowqa*.png

# Presentation decks are presenter material, not lab material - attendees only
# need the deploy/lab scripts and workbooks. Decks are distributed separately.
*.pptx

# The module bundle ships as a GitHub RELEASE ASSET, not in the repo -
# 64 MB in git history would bloat every clone. Built by tools/New-LabToolsBundle.ps1.
labtools-modules.zip

# Local editor / OS cruft
.DS_Store
Thumbs.db
*.tmp

# Keep the printable flowchart PDF (it's ours). If you prefer not to track
# generated PDFs, uncomment the next line and regenerate from the .svg:
# docs/diagnostic-flowchart.pdf
